apiVersion: v2
name: anvilkit-agent-mcp
description: The AnvilKit Agent MCP service (anvilkit-agent-mcp) - the service's own chart with its owner queue relay sidecar; environment values and the pinned deployment combination belong to the deploying repository.
type: application
version: 0.1.0
appVersion: "0.1.0"
